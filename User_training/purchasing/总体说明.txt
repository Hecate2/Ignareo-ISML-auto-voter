在config.py里填写需要监控的网页，需要抢的商品和顾客资料。如果需要增加性能，首先应考虑增加顾客资料份数，其次是考虑增加同时运行的进程数（可能导致一个顾客抢多份货）。在IgnaleoG.py中找到`portList=tuple([i for i in range(`这句， 修改55569为55570即可同时运行2个进程。
修改程序代码后必须重新运行才能使修改生效。
双击IgnaleoG.py即可运行。应当出现一个小黑框。保持小黑框持续运行！如果出现小黑框闪退，请从cmd运行python IgnaleoG.py检查是否有报错。一般都是pip安装库不完整，或者config.py语法错误导致闪退。
